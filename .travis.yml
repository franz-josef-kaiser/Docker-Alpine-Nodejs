---
language: ruby

cache:
    - bundler

sudo: false

rvm:
    - 2.3.0

gemfile: tests/Gemfile.ci

services:
    - docker

branches:
    only:
        - master

script: "bundle exec rspec -f p tests/Dockspec.rb"